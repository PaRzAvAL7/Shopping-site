<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Furniture Shop - Chair List</title>
    <style>
        /*Table styling */
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            border: 1px solid #ddd;
            padding: 8px;
            text-align: left;
        }
        th {
            background-color: #f2f2f2;
        }
    </style>
</head>


<!-- Table for the products --> 
<body> 
    <h1>Furniture Shop - Chair List</h1>
    <form action="/order" method="POST">
        <table>
            <tr>
                <th>Product</th>
                <th>Price</th>
                <th>Quantity</th>
            </tr>
            {{#each products}}
            <tr>
                <td>{{this.product}}</td>
                <td>${{this.price}}</td>
                <td>
                    <!-- Input for quantity, default is 0 -->
                    <input type="number" name="quantity{{@index}}" min="0" value="0">
                </td>
            </tr>
            {{/each}}
        </table>
        <!-- Submit button to send the order -->
        <button type="submit">Submit</button>
    </form>
</body>
</html>